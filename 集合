'''
    集合(set)
    集合列表非常相似
    不同点：
        1.集合中只能存储不可变对象
        2.集合中存储的对象是无序的（不是按照插入顺序保存）
        3.集合中不能出现重复元素
'''
#使用{}
#使用set()
s = {1,5,8,15}
print(s,type(s))
s = set() #空集合
#可以通过set（）将字典和序列转换成空集合
d = {'a':1,'b':2,'c':4}
s = set(d)
print(s)
#in 和 not in
#len()
#add()
s.add(10)
print(s)
#update()   将一个集合中的元素添加到当前集合中
#pop()
#remove()删除指定元素
