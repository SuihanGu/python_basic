my_tuple = ()
#空元组
my_tuple = (1,2,3,4,5)
#当元组不为空时，（）可以省略
my_tuple = 10,20,30
#如果元组不是空元组，那它至少要有一个括号
my_tuple = 10,
my_tuple = 10,20,30,40
a,b,c,d = my_tuple
print('a=',a)
#元组的解包（解构）
#解包指元组当中的每一个元素都赋值给变量
a,b,*c  = my_tuple
print(a,b)
#在对一个元组进行解包时，变量的数量必须和元组中元素的数量一致
#也可以在变量前边加一个*，这样变量，这样变量将会获取元组中所有剩下的元素
a,*b,c = my_tuple
print(a,c)
